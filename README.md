# Mnist-digit-recognition
MNIST Digit Recognition is a simple machine learning project where a model is trained to recognize handwritten digits (0–9) from images. It helps beginners understand the basics of image classification, model training, and accuracy evaluation using Python.
